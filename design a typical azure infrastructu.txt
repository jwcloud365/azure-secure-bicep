I want to design a typical azure infrastructure in a  azure subscription within some tenant, with a app service as front end, a databse as backend, separated by virtual nets, connected via private endpoints. The frontend is protected by a web application firewall. All other components should not be internet facing, so protected by NSG and connect through private endpoints. Now produce 2 files for me: a planning.md file containing high-level vision, architecture, constraints, tech stack, tools for this application, and a task.md file that lists all tasks needed in a bulleted list that can be used to monitor progress. The architecture consists of a drawing of this infrastructure and a set of bicep files to deploy it. The bicep files should be modular and have their functionalities documented in code. Also separate configuration file is needed for all parameters that need to be filled in in the bicep files. Part of the results need be a script that deploys the components into azure using the bicep files, and test scripts that can be used to test the deployment of each individual component. Make your assumptions explicit. 